# Build artifacts
target/

# Git
.git/
.gitignore

# CI / CD
.github/

# IDE & tooling
.idea/
.claude/
.mcp.json

# Local dev
docker-compose.yml
docker/

# Infrastructure manifests (not part of the Rust build)
infra/argocd/
infra/charts/
infra/kafka/
infra/manifests/
infra/vault/

# Secrets
.vault-init.json

# Docs
CLAUDE.md
*.md

# Note: migrations are intentionally NOT excluded here.
# sqlx::migrate!("./migrations") is a compile-time macro that embeds SQL
# files into the binary. Excluding them from the build context breaks the build.
