# ServiceAccount used by the ClusterSecretStore to authenticate with Vault's
# Kubernetes auth method.  Vault's nexus role is bound to this service account
# name and the nexus namespace.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: nexus
  namespace: nexus
